# クラウド同期を使う場合に設定する。未設定でもアプリは動作する(ローカル保存のみ)。
# AWS(DynamoDB + Lambda)で構築した同期APIのURL。
# infra/sync.yaml をデプロイして得られる出力 SyncApiUrl の値を設定する。
VITE_SYNC_API_URL=https://xxxxxxxxxxxxxxxxxxxxxxxxxx.lambda-url.ap-northeast-1.on.aws/
